{
  "info": {
    "name": "Circle — demo flow (v2 Ajo)",
    "description": "6 deliberate sends, each one API contract. Flow is sequential: wallet funds the circle, the circle's daily step is what you pay. Hit Send, read the response and the Tests tab, then move on.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:3000" },
    { "key": "tokenA", "value": "" },
    { "key": "tokenB", "value": "" },
    { "key": "circleId", "value": "" },
    { "key": "idemKey", "value": "11111111-2222-4333-8444-555555555555" }
  ],
  "item": [
    {
      "name": "1. Dev login — Ada (creator)",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/auth/dev-login",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "body": { "mode": "raw", "raw": "{\"email\":\"ada@circle.com\",\"name\":\"Adaeze\"}" }
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "pm.test('Ada logged in — tokenA set', () => {",
        "  const j = pm.response.json(); pm.expect(j.accessToken).to.be.a('string');",
        "  pm.collectionVariables.set('tokenA', j.accessToken);",
        "});",
        "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/auth/dev-login', method: 'POST', header: { 'Content-Type': 'application/json' }, body: { mode: 'raw', raw: JSON.stringify({ email: 'james@circle.com', name: 'James' }) } }, (err, res) => {",
        "  pm.collectionVariables.set('tokenB', res.json().accessToken);",
        "  pm.test('James token ready for later', () => pm.expect(res.json().accessToken).to.be.a('string'));",
        "});"
      ]}}]
    },
    {
      "name": "2. Wallet — derived balance",
      "request": {
        "method": "GET",
        "url": "{{baseUrl}}/wallet",
        "header": [{ "key": "Authorization", "value": "Bearer {{tokenA}}" }]
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "pm.test('Wallet is derived — positive balance', () => {",
        "  const w = pm.response.json(); pm.expect(w.balance).to.be.a('number'); pm.expect(w.balance).to.be.above(0);",
        "});"
      ]}}]
    },
    {
      "name": "3. Create circle — 10k daily, 2 members, twice weekly",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles",
        "header": [{ "key": "Content-Type", "value": "application/json" }, { "key": "Authorization", "value": "Bearer {{tokenA}}" }],
        "body": { "mode": "raw", "raw": "{\"name\":\"Video Ajo\",\"contributionAmount\":10000,\"targetMembers\":2,\"contributionsPerWeek\":2,\"cycleLengthDays\":7}" }
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "const c = pm.response.json();",
        "pm.test('Created forming — daily 10k, pot will be 140k', () => { pm.expect(c.status).to.eql('forming'); pm.expect(c.contributionAmount).to.eql(10000); pm.expect(c.goalAmount).to.eql(140000); });",
        "pm.collectionVariables.set('circleId', c.id);",
        "const cid = c.id, tokA = pm.collectionVariables.get('tokenA'), tokB = pm.collectionVariables.get('tokenB'), base = pm.collectionVariables.get('baseUrl');",
        "pm.sendRequest({ url: base + '/circles/' + cid + '/invite', method: 'POST', header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + tokA }, body: { mode: 'raw', raw: JSON.stringify({ email: 'james@circle.com' }) } }, () => {",
        "  pm.sendRequest({ url: base + '/circles/' + cid + '/accept', method: 'POST', header: { Authorization: 'Bearer ' + tokB } }, () => {",
        "    pm.sendRequest({ url: base + '/circles/' + cid + '/activate', method: 'POST', header: { Authorization: 'Bearer ' + tokA } }, (err, res) => {",
        "      pm.test('Activated — roster frozen, cycle 1 collecting', () => pm.expect(res.json().status).to.eql('active'));",
        "    });",
        "  });",
        "});"
      ]}}]
    },
    {
      "name": "4. Contribute — one daily step (10k) with idempotency",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles/{{circleId}}/contribute",
        "header": [{ "key": "Content-Type", "value": "application/json" }, { "key": "Authorization", "value": "Bearer {{tokenA}}" }],
        "body": { "mode": "raw", "raw": "{\"amount\":10000,\"idempotencyKey\":\"{{idemKey}}\"}" }
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "pm.test('Contributed one daily step — 10k', () => {",
        "  const r = pm.response.json(); pm.expect(r.replayed).to.eql(false); pm.expect(r.entry.amount).to.eql('10000');",
        "});"
      ]}}]
    },
    {
      "name": "5. Replay same tap — no double charge",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles/{{circleId}}/contribute",
        "header": [{ "key": "Content-Type", "value": "application/json" }, { "key": "Authorization", "value": "Bearer {{tokenA}}" }],
        "body": { "mode": "raw", "raw": "{\"amount\":10000,\"idempotencyKey\":\"{{idemKey}}\"}" }
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "pm.test('Replay — same entry', () => pm.expect(pm.response.json().replayed).to.eql(true));",
        "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/wallet/transactions?limit=100', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') } }, (err, res) => {",
        "  const cid = pm.collectionVariables.get('circleId');",
        "  const hits = res.json().data.filter(t => String(t.amount) === '-10000' && t.relatedCircleId === cid);",
        "  pm.test('Exactly one 10k debit for this circle despite two writes', () => pm.expect(hits).to.have.lengthOf(1));",
        "});"
      ]}}]
    },
    {
      "name": "6. Authorisation — outsider and no-token both fail",
      "request": {
        "method": "GET",
        "url": "{{baseUrl}}/circles/{{circleId}}",
        "header": [{ "key": "Authorization", "value": "Bearer invalid.token.here" }]
      },
      "event": [{ "listen": "test", "script": { "exec": [
        "pm.test('Bad token is rejected', () => pm.expect(pm.response.code).to.eql(401));",
        "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/' + pm.collectionVariables.get('circleId'), method: 'GET' }, (err, res) => {",
        "  pm.test('No token is rejected', () => pm.expect(res.code).to.eql(401));",
        "  pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/auth/dev-login', method: 'POST', header: { 'Content-Type': 'application/json' }, body: { mode: 'raw', raw: JSON.stringify({ email: 'outsider@circle.com', name: 'Outsider' }) } }, (err2, res2) => {",
        "    const t = res2.json().accessToken;",
        "    pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/' + pm.collectionVariables.get('circleId'), method: 'GET', header: { Authorization: 'Bearer ' + t } }, (err3, res3) => {",
        "      pm.test('Outsider cannot read the circle', () => pm.expect(res3.code).to.eql(403));",
        "    });",
        "  });",
        "});"
      ]}}]
    }
  ]
}
