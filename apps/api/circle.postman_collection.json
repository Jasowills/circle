{
  "info": {
    "name": "Circle \u2014 demo flow (v2 Ajo)",
    "description": "5 contracts, one Send per story. Each proves a real backend guarantee with a visible assertion. Hit Send, pause, let the response land, read the Tests tab, then move on.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000"
    },
    {
      "key": "tokenA",
      "value": ""
    },
    {
      "key": "tokenB",
      "value": ""
    },
    {
      "key": "circleId",
      "value": ""
    },
    {
      "key": "idemKey",
      "value": "11111111-2222-4333-8444-555555555555"
    }
  ],
  "item": [
    {
      "name": "1. Auth + Wallet \u2014 who we are and what we have",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/auth/dev-login",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"email\":\"ada@circle.com\",\"name\":\"Adaeze\"}"
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Ada logged in', () => {",
              "  const j = pm.response.json();",
              "  pm.expect(j.accessToken).to.be.a('string');",
              "  pm.collectionVariables.set('tokenA', j.accessToken);",
              "});",
              "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/auth/dev-login', method: 'POST', header: { 'Content-Type': 'application/json' }, body: { mode: 'raw', raw: JSON.stringify({ email: 'james@circle.com', name: 'James' }) } }, (err, res) => {",
              "  const j = res.json();",
              "  pm.collectionVariables.set('tokenB', j.accessToken);",
              "  pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/wallet', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') } }, (err2, res2) => {",
              "    const w = res2.json();",
              "    pm.test('Wallet is derived \u2014 has a positive balance', () => {",
              "      pm.expect(w.balance).to.be.a('number'); pm.expect(w.balance).to.be.above(0);",
              "    });",
              "  });",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "2. Circle lifecycle \u2014 create, invite, accept, lock",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Authorization",
            "value": "Bearer {{tokenA}}"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"name\":\"Video Ajo\",\"contributionAmount\":10000,\"targetMembers\":2,\"contributionsPerWeek\":2,\"cycleLengthDays\":7}"
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "const c = pm.response.json();",
              "pm.test('Created forming \u2014 140k pot computed', () => { pm.expect(c.status).to.eql('forming'); pm.expect(c.goalAmount).to.eql(140000); });",
              "pm.collectionVariables.set('circleId', c.id);",
              "const cid = c.id, tokA = pm.collectionVariables.get('tokenA'), tokB = pm.collectionVariables.get('tokenB'), base = pm.collectionVariables.get('baseUrl');",
              "pm.sendRequest({ url: base + '/circles/' + cid + '/invite', method: 'POST', header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + tokA }, body: { mode: 'raw', raw: JSON.stringify({ email: 'james@circle.com' }) } }, () => {",
              "  pm.sendRequest({ url: base + '/circles/' + cid + '/accept', method: 'POST', header: { Authorization: 'Bearer ' + tokB } }, (err, res) => {",
              "    pm.test('Accepted \u2014 still forming, roster not yet frozen', () => pm.expect(res.json().status).to.eql('forming'));",
              "    pm.sendRequest({ url: base + '/circles/' + cid + '/activate', method: 'POST', header: { Authorization: 'Bearer ' + tokA } }, (err2, res2) => {",
              "      pm.test('Creator activated \u2014 active, fair one-time draw', () => pm.expect(res2.json().status).to.eql('active'));",
              "      pm.sendRequest({ url: base + '/circles/' + cid + '/cycles', method: 'GET', header: { Authorization: 'Bearer ' + tokA } }, (err3, res3) => {",
              "        const j = res3.json();",
              "        pm.test('Rotation locked: 2 cycles, cycle 1 collecting, 140k', () => { pm.expect(j).to.have.lengthOf(2); pm.expect(j[0].status).to.eql('collecting'); pm.expect(j[0].targetPot).to.eql(140000); });",
              "      });",
              "    });",
              "  });",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "3. Contribute \u2014 one atomic wallet-debit plus pot-credit",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles/{{circleId}}/contribute",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Authorization",
            "value": "Bearer {{tokenA}}"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"amount\":140000,\"idempotencyKey\":\"{{idemKey}}\"}"
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Contributed \u2014 pot filled atomically', () => {",
              "  const r = pm.response.json();",
              "  pm.expect(r.replayed).to.eql(false);",
              "  pm.expect(r.circle.balance).to.eql(140000);",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "4. Idempotency \u2014 same tap, no double charge + payout observed",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles/{{circleId}}/contribute",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Authorization",
            "value": "Bearer {{tokenA}}"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"amount\":140000,\"idempotencyKey\":\"{{idemKey}}\"}"
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Replay \u2014 same entry, no duplicate charge', () => {",
              "  pm.expect(pm.response.json().replayed).to.eql(true);",
              "});",
              "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/wallet/transactions?limit=100', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') } }, (err, res) => {",
              "  const cid = pm.collectionVariables.get('circleId');",
              "  const hits = res.json().data.filter(t => String(t.amount) === '-140000' && t.relatedCircleId === cid);",
              "  pm.test('Exactly one 140k debit for this circle despite two writes', () => pm.expect(hits).to.have.lengthOf(1));",
              "  pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/' + cid + '/cycles', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') } }, (err2, res2) => {",
              "    const cs = res2.json();",
              "    pm.test('Cycle advanced \u2014 cycle 1 paid, cycle 2 collecting', () => {",
              "      pm.expect(cs[0].status).to.eql('payout_completed');",
              "      pm.expect(cs[1].status).to.eql('collecting');",
              "    });",
              "  });",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "5. Guards, discover, and timeline",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/circles/{{circleId}}/contribute",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Authorization",
            "value": "Bearer {{tokenA}}"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\"amount\":10000,\"idempotencyKey\":\"{{$guid}}\"}"
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('New cycle accepts a fresh tap', () => pm.expect(pm.response.code).to.eql(200));",
              "pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/' + pm.collectionVariables.get('circleId') + '/contribute', method: 'POST', header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') }, body: { mode: 'raw', raw: JSON.stringify({ amount: 10000, idempotencyKey: pm.variables.replaceIn('{{$guid}}') }) } }, (err, res) => {",
              "  pm.test('Cadence blocks the immediate re-tap', () => {",
              "    pm.expect(res.code).to.eql(400);",
              "    pm.expect(res.json().message).to.match(/Next contribution opens in/);",
              "  });",
              "  pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/' + pm.collectionVariables.get('circleId') + '/contribute', method: 'POST', header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + pm.collectionVariables.get('tokenB') }, body: { mode: 'raw', raw: JSON.stringify({ amount: 100000000, idempotencyKey: pm.variables.replaceIn('{{$guid}}') }) } }, (err2, res2) => {",
              "    pm.test('Overdraw rejected', () => {",
              "      pm.expect(res2.code).to.eql(400);",
              "      pm.expect(res2.json().message).to.match(/Insufficient/);",
              "    });",
              "    pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/circles/discover', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenB') } }, (err3, res3) => {",
              "      pm.test('Discover lists open circles', () => pm.expect(res3.json()).to.be.an('array'));",
              "      pm.sendRequest({ url: pm.collectionVariables.get('baseUrl') + '/notifications', method: 'GET', header: { Authorization: 'Bearer ' + pm.collectionVariables.get('tokenA') } }, (err4, res4) => {",
              "        pm.test('Timeline lives', () => pm.expect(res4.json()).to.be.an('array'));",
              "      });",
              "    });",
              "  });",
              "});"
            ]
          }
        }
      ]
    }
  ]
}